spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/desafio_db
    username: root
    password: root

  jpa:
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
      ddl-auto: update
    show-sql: true